# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Pinned' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Pinned
  pod 'Firebase/Auth'
  pod 'Firebase/Database'
  pod 'GoogleSignIn'  

  target 'PinnedTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'PinnedUITests' do
    # Pods for testing
  end

end
